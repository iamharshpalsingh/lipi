# A module used by stdlib.lipi (files starting with _ aren't run as golden tests)
version = "1.0"

shout(text)
    return text.upper() + "!"

secretHelper()
    return "hidden"

export version, shout
