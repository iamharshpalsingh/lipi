[package]
name = "lipi"
description = "The Lipi command-line tool: run, check, test and create Lipi projects"
version.workspace = true
edition.workspace = true

[[bin]]
name = "lipi"
path = "src/main.rs"

[dependencies]
lipi_compiler = { path = "../compiler" }
lipi_runtime = { path = "../runtime" }
serde_json = "1"
